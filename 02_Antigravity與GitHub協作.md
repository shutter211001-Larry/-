# Chapter 02: Antigravity 與 GitHub 協作

## 1. 痛點場景

👨 **你**：專案要交接，但我桌面全是「角色立繪_絕對最終版.psd」... 萬一刪錯圖層就全沒了！
![場景一：滿桌布最終版](./images/ch2_scene1.png)

👨 **你**：完蛋了！！我不小心把圖層全部平面化還按到儲存了！這下沒救了... 這時候如果你不知道甚麼叫做 Git，你就只能摸摸鼻子重新做一版了！
![場景二：崩潰抱頭](./images/ch2_scene2.png)
👨 **你**：網路上說用 Git 備份，就再也不怕老闆說要改回「第一版」。結果我滿懷希望打開教學，一看到那黑壓壓的「終端機」和滿滿的英文報錯... 救命，我只是做行銷設計的，為什麼要學工程師寫程式啊！
![場景三：被終端機報錯嚇瘋](./images/ch2_scene3.png)

---

## 2. 階段一：時光旅行的概念

### 2.1 觀念一：GitHub 是設計專案的時空旅行器
*   **這不是工程師的專利**：老闆看完 V5 後說「還是上週一的 V1 比較好」。如果沒有 GitHub，你只能在垃圾桶裡挖檔案。
*   **無限 Ctrl+Z (Mac 為 Cmd+Z)**：有了版控，只需一個按鍵，整個專案瞬間時光倒流回 V1 的完美狀態！就算不小心平面化存檔了也能救。

### 2.2 實作一：平面化儲存慘案
*(預計時間：20 分鐘)*
*   打開 Photoshop 的 **【圖層面板】** ➔ 在根圖層點擊右鍵選擇 **【影像平面化 (Flatten Image)】** ➔ 按下 **【Ctrl + S (Mac 為 Cmd + S)】** 儲存 ➔ **【關閉檔案】**。

> 重新打開檔案，體會一下無法按下 **Ctrl+Z (Mac 為 Cmd+Z)** 救回心血的絕望感。
> 雖然知道要用 Git 救命，但打開終端機看到一堆英文指令 (`git commit` 等)，只覺得頭昏眼花根本不想學！這就需要下一個觀念：讓 Agent 代勞！

> **🚨 警告**：剛剛的實作只是為了讓各位體驗「痛點」。千萬不要拿真實的公司專案跟著做！如果在公司專案上真的平面化存檔... 你會需要的是時光機，不是 GitHub。

---

*(2.2 實作一結束)*
☕ **【中場休息 10 分鐘】平面化存檔是不是很可怕？先喝口水壓壓驚！**

*(休息回來後...)*
👨 **你**：如果不用 GitHub 版控，我真的會被平面化存檔給嚇死。但 Git 指令那麼難，我要怎麼辦？

## 3. 階段二：終端機自動化 (Agent 代勞版控)

### 3.1 觀念二：動口不動手的版控
*   **動口不動手**：很多人不學 Git 是因為指令太難記。現在透過 Antigravity，你只要說「幫我備份這個版本的圖」，Agent 會自動幫你敲打終端機指令並上傳。

**💬 示範指令**：
> 幫我把剛畫好的圖 commit 存檔，並自動加上 "修改人物臉部光影" 的版本紀錄推送到 GitHub。
> *(註：Commit 就像是為當前的畫布狀態拍一張「拍立得存檔 (Snapshot)」，並在照片背面寫下修改備註，未來隨時可以拿出來還原。)*

### 3.2 實作二：見證一鍵備份與還原
*(預計時間：20 分鐘)*
*   點擊 **【Antigravity Chat】分頁** ➔ 輸入：`「幫我把專案 commit 備份到 GitHub」` ➔ (再次製造平面化慘案) ➔ 輸入：`「幫我還原到上一個版本」`。

> 重新打開檔案，見證圖層一鍵復活的時光倒流魔術！

---
👨 **你**：這太神了！但我不想把那些超肥大、或者包含機密的草稿檔也上傳上去，可以過濾掉嗎？

## 4. 階段三：專案防護網 (.gitignore)

### 4.1 觀念三：假裝沒看到的隱形斗篷
*   `.gitignore` 是一個純文字檔，裡面寫的任何檔名或路徑，Agent (與 Git) 都會假裝沒看到，絕對不會把它們上傳到網路上。這對於保護機密或是過濾掉太肥大的檔案（如圖層很多的設計檔）非常重要！

**📄 .gitignore 撰寫範例與萬用字元 `*` 的強大功能**：
`*` 代表「任何字元、任意長度」。這就像是拿著大聲公喊「所有叫 OOO 的通通給我站出來」。

```text
# 1. 利用萬用字元 (*) 忽略特定副檔名
# 只要檔案結尾是 .psd，全部忽略！
*.psd
*.psb
*.ai

# 2. 忽略特定資料夾內的所有東西
/assets/private/

# 3. 忽略檔名包含特定關鍵字的檔案
*secret*

# 4. 【例外條款】使用驚嘆號 (!) 建立白名單
!final_logo.psd
```

### 4.2 實作三：建立防護網
*(預計時間：20 分鐘)*
*   在專案根目錄（也就是左側檔案總管最外層的資料夾，點擊右上角的「新增檔案」圖示）建立 `.gitignore` 檔案 ➔ 練習輸入 `*.psd` ➔ 再次請 Agent 幫你備份專案。
*   你會發現 `.psd` 檔案被成功忽略，沒有被推送到 GitHub 上。

### ⚠️ 實作避坑 TIP
*   **資安慘劇**：一句自動存檔可能把商業機密公開，永遠記得先寫好 `.gitignore`。
*   **版本衝突與無腦覆蓋 (Force Push)**：當你和同事同時修改同一個檔案時，Git 會發出「版本衝突 (Conflict)」的警告，這代表系統不知道該聽誰的。此時千萬不要讓 Agent 自動執行 Force Push (強制推送) 來解決問題。這相當於「撕毀別人的畫布」，不管三七二十一直接把同事畫好的圖撕掉，強行貼上你自己的版本，極度危險。

---

## 5. 階段四：視覺化時空機 (GitHub Desktop)

### 5.1 觀念四：讓版本紀錄一目了然
雖然我們可以讓 Agent 代勞所有的備份與還原，但對於設計師來說，有時候還是希望能「親眼看到」所有的歷史版本。這時候我們在第 0 章安裝的 **GitHub Desktop** 就派上用場了！它就像是專案的「視覺化行車紀錄器」。

### 5.2 實作四：開啟你的視覺化時空機
*(預計時間：10 分鐘)*
*   打開電腦上的 **【GitHub Desktop】** 程式 ➔ 選擇 **【Add Local Repository (加入本機儲存庫)】** ➔ 選擇你現在的專案資料夾。
*   點擊左上角的 **【History (歷史紀錄)】** 標籤。

> 🚀 **確認成果**：你會看到畫面上清楚列出剛剛 Agent 幫你自動存檔的「修改人物臉部光影」等紀錄！未來如果你想手動回到某個版本，只要對著那條紀錄按右鍵選擇「Revert」就可以了，完全不用打任何一行程式碼！
